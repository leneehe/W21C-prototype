<h4 class="font-weight-bold py-3 mb-4">
  <span class="text-muted font-weight-light">Plan / Keep me Organized /</span> Event
</h4>
<div class="col-sm-6">
  <div class="card">
    <div class="card-header">
      <h3 class="card-header-title"><%= @event.name %></h3>
      <p>
        <%= @event.event_type %>
      </p>
    </div>
    <div class="card-body">
      <p>
        <% if @event.end %>
          <b>Time: </b><%= @event.start.to_formatted_s(:long) %> – <%= @event.end.to_formatted_s(:long) %>
        <% else %>
          <b>Time: </b><%= @event.start.to_formatted_s(:long) %>
        <% end %>
      </p>
      <p>
        <b>Location: </b><%= @event.location %>
      </p>
      <p>
        <b>Repeat: </b><%= @event.frequency %>
      </p>

    </div>
  </div>
</div>

<div class="col-sm-6">
  <div class="card">
    <div class="card-header">
      <h3 class="card-header-title">Journal Entries</h3>
    </div>
    <div class="card-body">
    </div>
  </div>
</div>
